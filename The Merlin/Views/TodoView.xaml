<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="The_Merlin.Views.TodoView">
    <Grid Padding="5,2,5,2">
        <Grid.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding ClickedFromGrid}"/>
        </Grid.GestureRecognizers>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <VerticalStackLayout Grid.Column="0" HorizontalOptions="Fill">
            <HorizontalStackLayout>
                <Label x:Name="isItNoted" Text="📃" IsVisible="False"></Label>
                <Label 
            x:Name="TitleLabel"
            VerticalOptions="Center" 
            HorizontalOptions="Fill"
            Margin="25,5,25,5"/>
            </HorizontalStackLayout>
            <Editor x:Name="TextEditor" Margin="0,0,0,5" IsVisible="False" TextChanged="TextEditor_TextChanged"/>
        </VerticalStackLayout>
    </Grid>
</ContentView>
