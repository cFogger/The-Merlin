<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="The_Merlin.Views.TodoDefListView"
             Title="TodoDefListView">
    <VerticalStackLayout>
        <CollectionView ItemsSource="{Binding TodoDefItems}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout>
                        <Button
                            Text="{Binding TodoDefText}"
                            Command="{Binding NavigateToDetail}"
                            Margin="5"/>
                        <Button Text="Delete Todo Definition"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, 
                                Path=BindingContext.DeleteCommand}"
                            CommandParameter="{Binding .}"
                            Margin="10"/>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button
            Text="Add New Todo Definition"
            Command="{Binding AddNewTodoDefCommand}"
            Margin="10"/>
    </VerticalStackLayout>
</ContentPage>