<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="The_Merlin.Views.TodoDefListView"
             Title="TodoDefListView">
    <VerticalStackLayout>
        <CollectionView ItemsSource="{Binding TodoDefItems}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                        <Border Margin="10,0" Padding="2" StrokeThickness=".1" BackgroundColor="{StaticResource Gray950}">
                            <Grid Padding="3" ColumnSpacing="3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="80"></ColumnDefinition>
                                    <ColumnDefinition Width="80"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Label
                                Text="{Binding TodoDefText}"
                                VerticalOptions="Center"
                                Grid.Column="0"
                                Margin="5"/>
                                <Button 
                                Text="Edit"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, 
                                Path=BindingContext.NavigateToDetail}"
                                CommandParameter="{Binding .}"                                
                                Grid.Column="1"/>
                                <Button 
                                Text="Delete"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, 
                                Path=BindingContext.DeleteCommand}"
                                CommandParameter="{Binding .}"
                                Grid.Column="2"/>
                            </Grid>
                        </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button
            Text="New"
            Command="{Binding AddNewTodoDefCommand}"
            Margin="10"/>
    </VerticalStackLayout>
</ContentPage>